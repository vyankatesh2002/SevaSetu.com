# Assets / Icons

Place custom SVG icons here for offline usage.

Currently SevaSetu uses:
- Font Awesome 6.5.0 (CDN) — in `prototype.html` / legacy pages
- Bootstrap Icons 1.10.5 (CDN) — in all module pages

If you need offline icons, download the selected SVGs and reference them with relative paths.

